<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Cần để tự quét được thư mục Downloads/vdy do app quay màn hình tạo ra (app khác tạo
         file, không đi qua MediaStore của app này) - trên Android 11+ (API 30+), việc đọc file
         nằm ngoài thư mục riêng của app trong bộ nhớ dùng chung BẮT BUỘC phải có quyền đặc biệt
         "Cho phép quản lý mọi tệp" (MANAGE_EXTERNAL_STORAGE), quyền thường (READ_EXTERNAL_STORAGE)
         không đủ để thấy file do app khác ghi ra ngoài MediaStore. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.NoActionBar"
        android:usesCleartextTraffic="false"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="unspecified"
            android:theme="@style/Theme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="unspecified"
            android:theme="@style/Theme.NoActionBar" />

    </application>
</manifest>
